# this config allows the @BeforeAll annotated method to be NON-static.
# alternatively use @TestInstance(TestInstance.Lifecycle.PER_CLASS) on every test class.
junit.jupiter.testinstance.lifecycle.default = per_class
